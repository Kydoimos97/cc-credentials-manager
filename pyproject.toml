[project]
name = "cc-cred"
version = "0.1.0"
description = "Claude credential manager and autonomous runner"
requires-python = ">=3.11"
dependencies = [
    "claude-agent-sdk>=0.0.1",
    "textual>=0.80.0",
    "click>=8.0.0",
    "rich>=13.0.0",
    "tzdata>=2024.1",
    "logspark[color]>=0.10.2",
    "httpx>=0.27.1",
]

[project.urls]
Repository = "https://github.com/Kydoimos97/cc-credentials-manager"

[project.scripts]
claude-auto = "cc_cred.cli_auto:main"
cc-creds    = "cc_cred.cli_creds:main"

[build-system]
requires = ["uv_build>=0.7"]
build-backend = "uv_build"
